'use strict';

var _activity = new Vue({
	el: '#activity',
	data: {
		activitvityIndex: 1,
		tagIndex: 1,
		statusIndex: 1,
		activityTag: 0,
		btnText: '马上报名',
		addressIndex: 'all',
		hasProvice: false,
		hasCity: false
	},
	methods: {
		changeActivity: function changeActivity(val) {
			this.activitvityIndex = val;
		},
		changeTag: function changeTag(val) {
			this.tagIndex = val;
		},
		changeStatus: function changeStatus(val) {
			this.statusIndex = val;
		},
		changeActivityTag: function changeActivityTag(val, status) {
			this.activityTag = val;
			if (status) {
				this.btnText = '精彩回顾';
			} else {
				this.btnText = '马上报名';
			}
		},
		changeAddress: function changeAddress(val) {
			this.addressIndex = val;

			if (val == 'provice') {
				this.hasProvice = true;
				this.hasCity = false;
			}

			if (val == 'city') {
				this.hasCity = true;
				this.hasProvice = false;
			}
		}
	},
	filters: {},
	mounted: function mounted() {}
});
//# sourceMappingURL=maps/activity.min.js.map
